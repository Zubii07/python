# Function created using an expression 'lambda' keyword. Syntax: lambda arguments: expression

'''def square(x):
    return x*x

print(square(5))
'''
# Same function can be written using lambda as:
square = lambda x: x*x
print(square(5))