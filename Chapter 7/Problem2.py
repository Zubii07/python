# Program to print all names of a person starts with S:

listOfNames = ['Sabir', 'Talha', 'Suleman','Ahad', 'Ahsan']

for names in listOfNames:
    if(names.startswith('S')):
        print(f"Hello, {names}" )