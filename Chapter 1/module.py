# A module is a file containing code written by somebody else (Usually)which can be imported and used in our programs.

# PIP is a package manager for python. you can use pip to install modules on your system. for example, install pyjokes and import it:

import pyjokes

jokes = pyjokes.get_joke()
print (jokes)